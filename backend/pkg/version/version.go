// Package version exposes release metadata for sidecar consumers.
package version

// Module is the Go import path consumers should pin with go get.
const Module = "github.com/madmmas/temflowral/backend"

// Version is the documented release train for this module tree.
// Bump when cutting tagged releases that hosts should depend on.
const Version = "0.1.0"
